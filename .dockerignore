.git
.env
.env.*
.hunknote
.idea/
temp/
results/
evals/
docs/
node_modules
.npm-cache
app-template/node_modules
app-template/dist
artifacts
output
coverage
**/__pycache__/
*.pyc
# Only the two committed Pi config files ship. Everything else Pi writes into the
# config directory under a local PI_CODING_AGENT_DIR -- auth.json (credentials on
# /login), models-store.json, sessions/, pi-debug.log, trust.json -- must never
# enter the build context; .gitignore is a separate mechanism Docker does not
# read. The judged image now bakes ENV PI_CODING_AGENT_DIR=/challenge/.pi-agent
# (organizer ruling, 2026-09-03: no organizer image for this track), so what
# ships here is exactly what the judged run reads -- keep it inert credential-wise.
.pi-agent/**
!.pi-agent/models.json
!.pi-agent/settings.json

# Written by the runner. prepareOutput() unlinks a stale copy at run start, so
# this is belt-and-braces; it still should not ride into the image.
result.json

# Reference-run copies for the submission (npm run submission). Not needed
# inside the judged image itself.
submission/
